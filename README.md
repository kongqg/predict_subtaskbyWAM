# predict_subtaskbyWAM

Subtask progress / done prediction code for meeting-room robot data.

The main package is `subtask_progress/`. See `subtask_progress/README.md` for
training, evaluation, V-JEPA feature extraction, and online monitor usage.
